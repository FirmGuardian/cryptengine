syntax = "proto3";
package messages;

import "common.proto";

message EncryptedFile {
    MType mtype = 1;
    repeated bytes recipientHashes = 2;
    bytes cipherKey = 3;
    bytes cipherNonce = 4;
    bytes encryptedData = 5;
}
